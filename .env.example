# Demo access gate: code required to view /demo (see app/demo/access).
# Set in Production + Preview on Vercel.
DEMO_ACCESS_CODE="vrieling2026"

# Required for OpenAI APIs: summarize (app/api/timeout/summarize) + task-from-summary (app/api/timeout/task-from-summary)
OPENAI_API_KEY=sk-...
